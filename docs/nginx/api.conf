# ==========================================================
#  NGINX - API uchun (backend)
#  Manzil: /etc/nginx/sites-available/api.igniteblog.com
#  Domeningizga moslab "api.igniteblog.com" ni o'zgartiring.
# ==========================================================

server {
    listen 80;
    server_name api.igniteblog.com;

    # Yuklanadigan fayl hajmi (rasmlar uchun)
    client_max_body_size 10M;

    location / {
        proxy_pass http://127.0.0.1:5000;   # backend PORT
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}
